# No Man's Sky 自动扫描器：Codex 项目指令

## 项目目标

为 Windows 版《无人深空》（No Man's Sky）开发一个前台自动化工具。第一版只实现以下两项功能：

1. 在欧几里得星系（Euclid Galaxy）中随机跃迁到可到达的恒星系。
2. 每次跃迁成功并完成加载后，自动扫描整个恒星系。

## 第一版范围限制

除上述两项功能以及保障其安全运行所必需的启动、停止、日志和错误处理外，不得自行增加功能。

以下功能明确不属于第一版：

- 游戏在后台运行时继续自动化。
- 用户挂机时同时操作其他软件。
- 筛选、识别或评价类似地球的行星。
- 保存行星、恒星系坐标或传送门符文。
- 自动命名、上传或修改任何发现。
- 前往、接近或穿越银河中心。
- 切换银河，或使用可能把玩家送出欧几里得的随机传送方式。
- 自动战斗、补充资源、购买物品或修改存档。

如果后续需要上述功能，必须由用户明确提出并作为新的开发阶段处理。

## 开发前必须确认

首次编写实际自动化代码前，先阅读 `PROJECT_CONTEXT.md`，并向用户确认其中“开发前待确认信息”列出的关键参数。不要猜测分辨率、按键、游戏语言、加载时间、跃迁载具或扫描方式。

## 实现原则

- 第一版可以使用游戏位于前台并占用输入的运行方式；后续需尝试后台控制游戏。
- 只使用普通银河地图中的自由探索（Free Explore）和常规星际跃迁。
- 不选择银河中心（Galactic Core）路线，不调用银河切换或 Relic Gate 类功能。
- “随机跃迁”定义为：从当前银河地图中可到达、可确认的候选恒星中进行伪随机选择；不要求对整个欧几里得星系实现数学上的均匀随机分布。
- 优先使用画面状态识别和明确的状态机，不要只依赖固定鼠标坐标与固定延迟。
- 所有必须使用的坐标、等待时间、按键和置信度阈值应放入外部配置文件，避免硬编码。
- 无法确认当前画面状态、跃迁是否成功或扫描是否完成时，优先安全停止，不要继续盲目点击。
- 必须提供启动、暂停/继续、紧急停止功能。
- 必须记录基本运行日志，包括开始时间、跃迁次数、扫描次数、重试、错误和停止原因。
- 除非用户明确同意，不要求管理员权限，不安装来历不明的可执行文件，也不修改游戏存档。
- 不自动安装 Meta Mod 或其他游戏模组；如果方案依赖模组，先说明并取得用户确认。

## 技术与交付偏好

- 首选能够打包为单个 Windows `.exe` 的方案，并同时保留源代码和配置文件。
- 如果可靠打包为 `.exe` 的成本过高，可以提供双击运行的 `.bat` 启动器，但必须清楚列出运行环境和依赖。
- Python 方案优先考虑 OpenCV、窗口状态检测和可打包工具；AutoHotkey 方案必须使用 AutoHotkey v2，不得直接沿用旧版 v1 语法。
- 打包版本不得把用户机器的绝对路径写死在程序中。
- 所有外部依赖必须有明确版本，并从官方来源安装。

## 安全与验证

- 开发阶段先提供“单次跃迁＋单次扫描”的测试模式，再开放循环模式。
- 循环模式应支持最大跃迁次数或最长运行时间，避免无限失控运行。
- 每次实际测试前提醒用户备份存档，并优先使用测试存档。
- 在执行鼠标或键盘操作前确认《无人深空》窗口存在且处于前台。
- 窗口失去焦点时立即暂停或安全停止，不得向其他软件发送输入。
- 对失败状态保存日志；如用户同意，可在错误时保存截图用于调试。
- 完成任何实现后，先运行适用的静态检查和非破坏性测试，再向用户报告结果与仍存在的限制。

## 协作方式

- 使用中文与用户沟通，技术名词可附英文。
- 先说明将要修改的文件和预期行为，再执行较大改动。
- 不隐藏不确定性；如果某个游戏界面、模组功能或版本兼容性未经实测，应明确标注。
- 保持修改范围紧凑，避免为第一版提前实现未来功能。
