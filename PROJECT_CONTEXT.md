# No Man's Sky 自动探索器：v1.1.1 发布上下文

更新：2026-09-09。该文件描述本正式版的需求和验证范围。用户已经明确授权后续开发阶段的原生后台方案、可选上传、图形界面与星系筛选，覆盖 AGENTS.md 中最初的前台第一版范围。用户要求发布 GitHub 正式版并附 Release。

## 当前功能

- 从普通银河地图中伪随机选择可到达的恒星系，执行货船常规跃迁，等待真实加载完成，再登记整个星系的行星发现。
- 图形界面及 F1 启动/暂停/继续；F2 开关自动上传，默认关闭；F3 停止后续动作。支持跃迁次数和时长上限，0 表示不限。
- 筛选默认关闭。启用时按光谱字母、数字、后缀、星系类别、地图标签和主导种族六组过滤；组内 OR、组间 AND。无人类别与其他种族可同时多选，不再清空或禁用种族组。种族选项为吉克、维吉恩、科尔瓦克、无主导种族。
- 筛选在连接前设置，每组至少勾选一项；每轮最多尝试256个候选，找不到匹配可达目标则停止，不放宽条件。地图标签在游戏已生成的面板中读取，跃迁前再次验证。
- 不发送键鼠输入，不依赖扫描室布局，不要求 Meta Mod，不读取或修改存档文件。所有原生调用都受固定版本、状态和线程检查约束。

## 环境和运行条件

Steam 公开分支，游戏内部版本170671，NMS.exe SHA-256 为 `ea7e5a29bbf931f96ae8dda81553353aab3431dc38e4ed7dfb66bdce100770e3`。开发测试使用 Windows11、2560×1600、150%显示缩放、默认键位、中文游戏及已建扫描室的货船。

发行包使用 Python3.12.10 x64 和 requirements.lock.txt 中固定依赖，可完整解压运行。每次测试先备份并使用测试存档；站在货船内部正常游戏画面。停止不会卸载注入的运行时，再次启动前必须完全退出并重新进入游戏。

## 验证证据

v1.1.1 的运行逻辑和原生配置与已测试的 rc3 一致，只更新程序版本、文档和发行元数据。

- rc2：10轮跃迁/扫描、48次行星提交；F/G、数字4～7、普通、科尔瓦克和水/气态巨行星限制下，全部10个放行目标符合条件，实际均为水；上传本地处理4批39条。证据 `research/star-filter-trial-20260909T0723.json`。
- rc3：2026-09-09 07:42～08:07，71轮跃迁/扫描、297次行星发现提交、948次候选尝试。逐轮流程完整，筛选违反0、错误0、日志丢弃0。无人/无主导种族46次，海盗25次（吉克7、维吉恩7、科尔瓦克11）；标签水56、不协15。12个无附加标签候选被复核拒绝。暂停后无动作，随后 stop_requested；上传全程关闭。证据 `research/star-filter-trial-20260909T0742.json`。
- 97项非游戏测试覆盖筛选组合、模型/CLI/UI、候选复核、状态机、命令通道和上传。rc3 的实际 Tk 窗口已在150%缩放下检查布局和选项行为。
- 发布流程再次执行 Ruff、97项测试、编译及便携包校验；本地离线核对当前游戏16个原生入口。GitHub Actions 从对应提交构建ZIP并附SHA-256发布。

这些证据不代表每个筛选选项都已经游戏实测：rc3 没有放行的废弃类别或气态巨行星标签，蠕虫未启用，种族和数字全选。关键动作均为前台，失焦完整循环和最小化仍需独立验证。发现提交次数不等于退出后存档持久化或网络服务器确认。

## 边界

动物、植物和矿物完整扫描研究已暂停，本版本不包含这些功能或试验性原生生成调用。除非用户明确重启该阶段，不要恢复。仍不添加自动命名、行星评价、坐标记录、补给、战斗、黑洞/Relic Gate 跃迁或银河切换。普通地图中心特殊目标仍被排除。
