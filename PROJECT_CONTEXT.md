> 发布快照：v1.1.0，2026-09-05 恢复。按用户要求发布加入动物、植物、矿物扫描之前的图形界面版本。以下为当时的需求和验证记录，后续生态研究不在本发布范围内。

# No Man's Sky 自动探索器：当前需求和开发状态

更新：2026-09-04。以下记录当前会话的明确决定，替代最初的前台方案。

## 当前目标

使用货船，从普通银河地图可到达的候选恒星中伪随机选择目标，执行常规跃迁，加载完成后扫描整个恒星系。用户按 F2 开启时，扫描后调用游戏“上传全部发现”；默认关闭。循环直到 F3 停止。

用户明确要求第一版尝试后台运行，并进一步要求研究直接调用游戏内部函数，避免键鼠操作和货船房间布局依赖。这是用户对原范围的变更；AGENTS.md 中旧的前台限制不再代表当前选择。没有因此获得安装整套 Meta Mod、修改存档或执行其他游戏动作的授权。

2026-09-03 用户明确把自动上传作为新阶段加入范围，并要求 F2 控制，因此 AGENTS.md 中原有“不上传”限制不再适用于该项。授权仅限扫描后“上传全部发现”，没有授权自动命名、修改发现或修改存档。仍不增加行星筛选、坐标记录、战斗、补给和存档解析；不用黑洞、Relic Gate、银河切换或其他随机传送替代普通货船跃迁。

## 已确认参数

| 参数 | 信息 | 来源 |
|---|---|---|
| 来源 | Steam 默认公开分支 | 用户 |
| 系统 | Windows 11 | 用户 |
| 分辨率 | 2560 × 1600 | 用户；本机设置一致 |
| Windows 缩放 | 150%，不是游戏 HUD 缩放 | 用户 |
| 窗口 | 无边框，非独占全屏 | 本机图形设置 |
| HUD | HUDZoom = 100 | 本机设置 |
| 语言 | Steam 为 schinese，游戏设置为 Default | 本机文件，未视觉确认 |
| 键盘 | 普通布局，未修改默认键位 | 用户 |
| 载具和位置 | 货船，已在欧几里得 | 用户 |
| 扫描室 | 已建好，控制台最近的房间右转 | 用户；原生方案不以路线为前提 |
| Meta Mod | 未安装 | 用户；本次未安装 |
| 热键 | F1 首次启动、之后暂停/继续；F2 开关自动上传，默认关闭；F3 停止 | 用户，2026-09-03 更新 |
| 时长 | 运行到 F3；开发时先测试单次 | 用户和测试要求 |
| 测试存档 | 11:45 前台两轮跃迁及扫描完成；11:46 已退出游戏，用户要求继续 | 用户及本次日志 |
| 最小化 | 后续希望支持，第一版不要求 | 用户 |
| 内部版本 | FileVersion / ProductVersion = 170671 | 本机 EXE |
| Steam buildid | 23857366 | 本机 manifest |
| 加载时间 | 手动校准 6.343 秒；10:49 自动测试 APPLOCALLOAD 阶段 8.344 秒；继续按状态等待 | 手动及自动日志 |

## 技术决定和当前成果

- 停止键鼠路线；已移除本次尚未完成的窗口消息和图像识别原型。
- 阅读公开 NMS.py、pyMHF 和 Meta Mod Lua 源码。没有获得游戏官方完整源码，公开逆向定义只覆盖部分函数。
- tools/audit_native.py 和 research/native-audit.json 提供只读 EXE 签名检查。唯一匹配不证明参数和运行行为正确。
- native/profile.json 固定当前 EXE 哈希、七个观察签名和参数，无机器安装路径。第七个已与手动普通跃迁对应；单次模式通过 single_trial.json 增加入口。
- native/observer.py 默认只观察，显式 --single / --loop 分别委托 SingleRun / ExplorationRun 执行动作；状态切换可记录游戏代码调用栈 RVA。不保存目标坐标、对象地址或存档内容。
- nms_scanner/launcher.py 默认只核查；--observe 加载观察探针，--single 加载单次版本，--loop 加载自动开图和循环版本。提供 --exe 时只做离线核查。使用 pyMHF 0.2.4，不加载整套 NMS.py 内部钩子，不安装 Meta Mod。
- start_probe.bat 是观察探针启动器，不是自动探索器。F1/F2/F3 使用全局 RegisterHotKey，不向游戏发送输入。
- nms_scanner/console_status.py 读取本次进程的新日志，在启动器显示就绪和 F1/F2/F3 状态；跳过旧记录，不向游戏发送命令。
- 已增加非游戏测试，覆盖版本变化、签名歧义、停止/暂停、队列上限、无效观察指针和回调保持参数/返回值。

2026-09-03 06:59 首次连接 PID 2780：Python 运行时已注入，但 pyMHF 导入失败，观察模块和游戏钩子尚未启用。随后游戏退出；Windows Application Error 1000 指向 python312.dll，异常 0xc0000005。没有执行跃迁、扫描或存档读写。

已停止对游戏的重试，并修正启动适配层：固定 PID、禁止框架退出回调结束进程、在无控制台环境导入依赖、合并初始化脚本并共享作用域、失败后记录错误并保持运行时驻留，避免进入解释器结束流程。没有修改安装的第三方包。

修正后先在自行编译的 RuntimeTestHost.exe 中验证共享初始化作用域、正常导入、故意失败时宿主存活、完整框架加载及真实原生函数钩子的返回值保持；当时另有 11 项单元测试通过。此阶段尚未再次连接游戏，不得把独立进程验证当作游戏验证成功。

随后用户重新打开测试存档并明确要求继续。第二次连接 PID 15000，于 07:12:29 成功加载 1 个观察模块和 6 个钩子，框架进入运行状态，观察心跳持续正常。已提示用户按 F1 并检查画面及前台/失焦时的更新；暂未要求跃迁或扫描。首次成功加载不等于后台兼容或自动探索已经通过验收。

用户随后反馈 F1 没有画面反应。日志已确认 F1 生效（active=true），应用和游戏状态更新计数持续增加，失去焦点时也有更新函数调用。当前探针没有界面提示，F1 仅开启记录，不会自动跃迁或扫描。尚需观察一次手动货船跃迁和整系扫描；更新函数被调用本身不能证明后台完整模拟或输入隔离已成立。

用户因额度中断退出游戏后要求继续。本轮只做离线分析和源代码修改，没有启动或连接游戏。现有日志记录 07:15:11 收到 F1、07:23:39 进入 APPSHUTDOWN；与用户手动退出相符。按相邻活动心跳统计，前台/失焦的游戏状态更新调用率中位数约 59.61/29.76 次每秒。原始证据摘要在 research/observer-session-20260903.json；没有跃迁或扫描记录。F2/F3 的游戏内响应仍未验收。

上一修订增加启动器状态反馈和候选跃迁的前后观察，随后用户自行完成了下述手动测试。

2026-09-03 10:02:44，PID 19068 成功加载 7 个观察钩子。用户手动完成普通货船跃迁和扫描，并按 F3、退出游戏。日志确认：跃迁入口 0x122C1B0 由 0x121E71D 调用一次并返回 true；随后 APPVIEW → APPLOCALLOAD → APPVIEW，加载阶段 6.343 秒、发起到加载返回 8.109 秒；扫描室交互触发 4 次 discovery_submit，首个调用栈位置均为 0xF8743F；F3 停止成功，dropped=0。证据保存于 research/manual-trial-20260903T1002.json。

跃迁发生在主线程 26040，扫描在模拟任务线程 26972；线程号只对此次进程有效。扫描调用栈包含 cGcSimulation::Update（入口 0x12A1060），其上层返回位置为 0x32EF23；该位置对应 Update(mode=0)。原先命名为 simulation_update 的观察点实际是 cGcGameState::Update，不能将它与 cGcSimulation::Update 混用。

本轮新增实验性单次自动测试，但没有重新启动或连接游戏：

- start_single_test.bat / --single：需用户先打开货船银河地图自由探索，F1 才开始自动选星、普通跃迁和扫描一次。现有 start_probe.bat 继续仅观察。
- nms_scanner/single_run.py：单次状态机，加载事件排队、F2 暂停、F3 阻止后续动作、超时、目标变更检查、完成后不重跑。原生动作只由游戏回调执行。
- nms_scanner/native_single.py：使用地图缓存的随机方向查询、原生选星和能力检查、已观察的跃迁入口，以及扫描室相同的逐行星提交。运行目标只驻留内存，不记录坐标。
- native/single_trial.json：增量配置，合计 9 个钩子、5 个额外调用入口，共 14 个签名在固定 EXE 中唯一匹配。新查询/选星函数与主动调用仍待游戏实测。
- 28 项非游戏测试、Ruff 和编译检查通过。用户关闭游戏后可直接关闭旧命令行窗口，日志不会丢失。

## 已找到的入口和缺口

2026-09-03 10:32 单次测试及修复：

- PID 4840 加载 9 个钩子，模式确认为 single。10:32:36 收到 F1，立即出现 `single_failed: application_not_observed`，candidate_attempts=0、warps=0、scans=0。不是热键未收到，也不是误用了观察模式。
- 随后用户手动跃迁和扫描：普通跃迁入口一次，加载状态顺序正常，扫描室一次、6 次发现提交；10:34:08 出现 APPSHUTDOWN。不能将这次手动成功计作自动成功。
- 当前 EXE 的 `cGcApplication::Update` 未使用传入 RCX，在 RVA 0x2DAAC8、0x2DAAE3 通过 RIP 相对 LEA 直接使用全局对象 RVA 0x6B44DA0。旧版 application_update 回调将不可靠的 this 写入 engine.application，是此次失败的实现原因。
- 已修正为校验上述两处指令并按模块基址定位对象；不再由更新参数覆盖。配置在 single_trial.json，磁盘核查及加载前均检查引用，不增加原生调用。反汇编在 research/application-update-disassembly.txt。
- 启动前错误现保存至 logs/launcher-errors.jsonl；重复加载仍被阻止，并明确提示必须完全退出游戏再重开。心跳增加自动阶段和自动计数，停止提示使用中文并显示计数。
- 用户随后确认第二次运行启动器前没有退出游戏，即在同一游戏进程重复启动。这符合现有 Python 运行时重复加载拦截的条件；下一次测试必须完全退出游戏到桌面再重开。旧报错原文未保存，但无需为确认是否重启过游戏再次询问用户。
- 修复后 33 项非游戏测试、Ruff、编译及当前 EXE 的 14 个函数入口和两处对象引用检查通过；没有启动或连接游戏。主动自动跃迁和扫描仍待验收。

2026-09-03 10:49 首次自动单次成功：PID 11316，F1 至完成约 10.312 秒，自动跃迁 1 次、扫描 1 次、6 次行星发现提交、候选尝试 1 次，无错误、dropped=0、无手动扫描室交互。用户明确确认跃迁和扫描全部成功。APPLOCALLOAD 阶段约 8.344 秒。核查时游戏仍运行，自动阶段保持 complete，不会因再次 F1 重跑；本轮只读取日志、更新文档和做离线入口分析，没有连接运行时或执行新游戏动作。证据在 research/automatic-trial-20260903T1049.json。

扫描：单次版在模拟 Update(mode=0) 返回阶段核对 APPVIEW、目标恒星系、自己的货船和当前太阳系对象，再逐行星调用扫描室相同的 SubmitDiscoveryData。10:49 主动提交及用户核对首次通过。

跃迁：单次版通过地图查询和选星函数让游戏持有目标对象，再次核对后先检查能力，再调用 RVA 0x122C1B0 普通跃迁分支。只有观察到 APPLOCALLOAD 后返回 APPVIEW 且实际恒星系匹配目标，才计入跃迁次数。10:49 主动调用首次通过。

兼容性：cTkFSMState::StateChange 的短签名有两处匹配，探针不使用它。SubmitDiscoveryData 的 Python 声明缺少返回类型，C++ 符号声明为 bool，探针按 bool 保留原返回值。游戏失焦后的模拟和输入行为仍需实测。

## 最小可测试版本计划

1. 手动校准及一次前台自动单次验收已完成，无需再把同样的前台单次作为未完成事项。
2. 就绪等待修订版已在 11:45 完成前台连续两轮，含重新开图、跃迁、加载核对、3/5 个行星扫描和次数上限停止，无需重复前台验收。
3. 下一次继续用 start_two_cycle_test.bat（2 次/180 秒），游戏不最小化、命令行前台：F1 后立即 F2，等 10 秒再 F2，后台完成第一轮后 F3。核查后台完整流程、暂停/继续、停止及不抢输入。关键事件新增发生时的焦点采样；原有后台心跳不能代替此项。连续启动器 start_explorer.bat 默认到 F3，最后评估打包。

本轮在用户退出游戏后新增：

- native/exploration.json：循环默认不限次数/时间、首轮和轮间等待 5 秒；新增队列调用 0x2998AF0 的完整 64 字节签名，解决原短签名歧义。循环共核查 15 个函数入口，仍为 9 个钩子。
- native_single.py 的 request_map：仅由 application_update 回调驱动，核对主线程游戏调用栈、APPVIEW/已开地图、自己货船内部及稳定位置、暂停标志、待处理跃迁和跃迁过渡、状态机所属关系及空请求队列。使用游戏队列函数复制 GALAXYMAP 标识，空 user_data、force=false；不直接改状态字段。
- 空队列标记从运行中的 RVA 0x3266F80 读取，与原生函数相同比较；不能把磁盘上的零值当作运行时标记。当前状态 +0x18 指向所属 FSM；FSM +0x18 是待处理状态标识。
- exploration_run.py 复用单次状态机：每轮重新获取对象，保留上一目标用于核对当前系统；每轮候选计数和行星索引重置，累计成功计数保留；失败即停。F2 冻结阶段/轮间计时，最长运行时间从 F1 起计算并包含暂停；F3 和时长检查阻止下一次原生调用。
- launcher.py 的 --loop 与上限覆盖会传给注入模块；新增 start_two_cycle_test.bat（2 次/180 秒）和 start_explorer.bat（配置默认不限，到 F3），保留 start_single_test.bat 的已开图单次方式和纯观察启动器。
- 48 项非游戏测试、Ruff、编译和 15 个入口的离线核查通过。没有启动或连接游戏；自动开图、连续循环、后台完整流程及活动中的 F2/F3 仍需游戏验收。

观察模式 F3 只停止记录；单次及循环模式 F3 阻止后续原生动作，不能撤销已经排队的开图或发起的跃迁。三种模式均需退出游戏才能完全卸载运行时和钩子。自动动作进行中的游戏内停止响应尚未验收。

来源和详细证据见 research/NATIVE_FINDINGS.md；运行步骤和限制见 README.md。

## 2026-09-04 完整循环、热键与自动上传

- 最新 PID 16956 日志确认两轮跃迁、两轮扫描、无错误；F1 旧映射下暂停约 13.5 秒后继续成功，F3 在次数上限完成后收到。关键动作事件的焦点采样均为 true，因此仍不是后台完整流程的客观证据。摘要在 `research/two-cycle-controls-trial-20260903T1153.json`。
- F1 已合并为首次启动及后续暂停/继续；F2 改为自动上传开关，每个新会话都从关闭开始；F3 保持终止。本地停止、完成或失败后热键不能重启同一会话。
- 通过 EXE 中 `UploadAndRewardAll@cGcFrontendPageDiscovery` RTTI、`UPLOAD_ALL` UI 分支及唯一直接调用，定位批量上传处理 RVA 0x706060。使用前端管理器构造的真实发现页对象；不伪造 UI 对象、网络客户端或发现数据。依据见 `research/UPLOAD_FINDINGS.md`。
- 自动上传只在整系扫描完成后、下一轮开始前，由主 application update 调用。先验证 APPVIEW、加载目标、自己的货船、暂停/跃迁/状态队列、前端 vtable、待上传集合稳定性和每条记录类型。F2 关闭及 F1 暂停、F3 停止会在原生调用前复查。
- 日志中的上传完成表示游戏原生“上传全部”处理函数已调用并清空本地可上传集合。2026-09-04 测试存档中 F2 开启后，程序自动跃迁 1 次、扫描 5 个行星、处理 16 条待上传记录并按一轮上限结束，无失败或丢弃；用户确认游戏内上传成功。摘要在 `research/upload-trial-20260904T0100.json`。
- 主程序版本定为 1.0.0，循环版合计核查 16 个入口。62 项非游戏测试、Ruff 和编译检查通过。启动器在注入前检查 pyMHF 固定本地端口，避免其他会话占用时半加载运行时。构建脚本生成含相对路径 EXE、Python 运行时、固定依赖、源码和配置的可搬动发行包。

## 11:24 / 11:26 地图停滞及就绪等待修订

用户先误启动 single，F1 后再手动开图；完全退出后重开 two_cycle，F1 自动进入地图，但两次均停留在星系页面。核查 PID 27336 / 29844：开图到发出跃迁分别仅 0.063 / 0.032 秒，warp_candidate 返回 true，均未出现 APPLOCALLOAD。single 在用户退出时记录 APPSHUTDOWN，loop 在 90 秒后 timeout_wait_load；0 次成功跃迁、0 次扫描。两种模式共有的就绪检查缺口，不能归因于用户点错启动器。

对仍运行的 PID 29844 仅使用 PROCESS_QUERY_INFORMATION | PROCESS_VM_READ 外部读取，核对 EXE 哈希及两处应用引用后采样三次。应用 GALAXYMAP、队列空、未暂停、货船内部位置 10、warp_request=3、warp_transition=0；地图计时持续增长。没有再次注入、调用游戏函数、写游戏内存或读取存档。证据在 research/stalled-map-20260903T1126.json；其中 camera_state/flag 是初始诊断字段名，对应后续分析的选星弹层状态。

修订 native_single.py、single_run.py 和 single_trial.json：每轮清除地图及选星计时；核对游戏持有的地图指针；至少观察 5 秒地图更新，并检查正常地图分支（0/3）、缓存和没有待处理跃迁；选择目标后至少等待 1 秒，且选星弹层状态为 3、选中标志为 1，再复核目标并跃迁。参数来自配置，时间只作为附加门槛。新增就绪字段及条件尚待游戏验证，特别是弹层状态条件可能需要据下一次日志校准。

日志新增 map_waiting / map_ready，不记录坐标或对象地址；timeout_wait_load 明确说明调用已返回但没有进入加载，不重复提交请求。53 项非游戏测试、Ruff、编译、15 个入口及两处应用引用离线核查通过。没有热更新运行中的旧会话，也没有进行新游戏动作。已告诉用户现场保存后可以 F3、退出游戏和命令行。

## 11:45 前台两轮成功及后台测试准备

PID 29792，11:45:11.112 F1 到 11:45:49.201 结束，monotonic 总耗时 38.078 秒。两次自动开图、正常跃迁、加载目标核对和扫描完成；扫描分别 3 / 5 个行星，每轮候选尝试 1，最终 max_warps，warps=2、scans=2，无失败、无手动扫描室交互、dropped=0。加载阶段 5.672 / 6.547 秒。两轮开图后等待约 5 秒，选星后等待 1.050 / 1.033 秒，均观察到弹层 state=3、selection=1 才提交跃迁。用户确认两次跃迁正常，扫描完成由日志确认。证据：research/two-cycle-trial-20260903T1145.json。

活动期间 38 个焦点心跳全为 true，无 F2/F3 事件；不属于后台或活动停止验收。11:46:16 日志 APPSHUTDOWN，本次进程检查没有 NMS 或启动器 Python 进程。没有重新启动或连接游戏。

为下一次验收，Observation 接收只读 focus_reader，在非高频事件产生时记录 game_foreground，避免使用异步写日志时的焦点误判；读取失败为 null，不影响停止控制。高频更新仍仅累计计数。保持原自动动作、游戏参数及现有启动器，更新启动器说明。55 项非游戏测试、Ruff 和编译通过；后续需用户进入测试存档进行上述后台与 F2/F3 测试。

## 2026-09-04 1.1.0 图形界面

- 新增 Windows 图形界面，显示连接/运行阶段、游戏窗口前后台状态、累计跃迁、扫描和上传记录，并提供最大跃迁次数、最长运行分钟数、启动/暂停/继续、自动上传和停止控制。F1/F2/F3 仍可并行使用。
- UI 通过每会话独立、带 32 位随机令牌和递增序号的本地 JSONL 命令文件调用原有 `SessionControls`；不发送键盘鼠标输入。无效令牌、未知命令、重复或倒序命令会被忽略。
- 自动探索器仍在独立进程中完成版本校验和注入。UI 只显示现有日志的当前 PID 事件；关闭窗口会先请求停止，再关闭外部启动进程。停止后的注入运行时仍需退出游戏才能完全卸载。
- UI 只承载已验收的随机跃迁、整系扫描及可选上传；未加入动物、植物或矿物的全行星扫描逻辑。
- 在 Win11 的真实 Tk 8.6.12 会话及 150% DPI 感知模式下完成窗口创建、完整布局预览和发行包资源检查。68 项非游戏测试、Ruff、编译及 ZIP 完整性检查通过；UI 按钮到游戏状态机的链路仍需一次测试存档验收。
